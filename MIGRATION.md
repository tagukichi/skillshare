# 将来の展開に向けた整理

SPEC.md「7. 実装順序」の全13ステップが完了した時点での引き継ぎ資料。
PoC で需要が確認できた場合に、SPEC.md「10. 備考（将来の展開）」へ進むための地図として使う。

---

## 1. 全体の地図

予約機能のロジックは `inc/booking/` に閉じ込めてある。テーマの表示処理と混ざっていないため、
プラグインとして切り出すことも、ロジックだけを別実装へ移すこともできる。

| ファイル | 責務 | 行数 | 移行時の扱い |
|---|---|---:|---|
| `inc/booking/install.php` | テーブル定義・固定ページ作成 | 390 | スキーマ定義として参照。実装は作り直し |
| `inc/booking/instructor.php` | 講師の申請・審査・ロール・ログイン・マイページの門番 | 816 | 認証は移行先の仕組みに置き換え。状態遷移は流用 |
| `inc/booking/course.php` | 講座の CRUD・画像・公開判定・ルーティング | 711 | ロジックはほぼそのまま移植可能 |
| `inc/booking/slot.php` | 予約枠の生成・仮押さえ・空き枠の抽出 | 948 | **中核。排他制御の考え方をそのまま持っていく** |
| `inc/booking/booking.php` | 予約レコードの読み書き・検索・集計 | 382 | ほぼそのまま移植可能 |
| `inc/booking/stripe.php` | Checkout セッション作成 | 275 | 公式 SDK へ置き換える前提 |
| `inc/booking/webhook.php` | Webhook 受信・署名検証・予約確定 | 214 | 署名検証は SDK へ置き換え。確定処理は流用 |
| `inc/booking/gcal.php` | ICS の生成・取得・解析・除外 | 821 | OAuth へ切り替えるなら取得部分のみ差し替え |
| `inc/booking/mail.php` | 各種メール | 303 | テンプレートとして流用 |
| `inc/admin/*.php` | 管理画面（審査・予約一覧・集計・CSV） | 1,037 | 移行先の管理画面に合わせて作り直し |
| `templates/`, `assets/js/calendar.js` | 画面 | 1,911 | 作り直し。カレンダーの表示ロジックは参考になる |

`functions.php` は require とテーマ設定だけを持ち、ロジックを持たない。

---

## 2. データモデル

`wp_ssb_` 接頭辞の4テーブルが唯一の正。WordPress の投稿テーブルには一切乗せていないため、
そのまま別のデータベースへ移せる。

```
instructors ──< courses ──< slots ──< bookings
```

| テーブル | 役割 | 移行時の注意 |
|---|---|---|
| `wp_ssb_instructors` | 講師。`user_id` で WP ユーザーと紐づく | 認証を移行先に移すなら `user_id` の対応表が要る |
| `wp_ssb_courses` | 講座。`image_id` は WP の添付ID | 画像は添付IDではなく URL で持ち出す |
| `wp_ssb_slots` | 予約枠。仮押さえの状態を持つ | `hold_token` は移行不要（一時的な値） |
| `wp_ssb_bookings` | 予約と決済履歴。`stripe_session_id` は一意 | **決済履歴なので消さない** |

### 日時の扱い

すべての `DATETIME` は**サイト設定のタイムゾーン（Asia/Tokyo）の壁時計**で保存している。
UTC ではない。移行時は次のいずれかを決めること。

- そのまま壁時計として引き継ぐ（現状維持）
- UTC に正規化する（推奨。その場合は `Asia/Tokyo` として解釈して変換する）

表示は必ず `mysql2date()` を通している。`strtotime()` を挟むと UTC として解釈され9時間ずれるため、
移行先でも同じ罠に注意。

### エクスポート

管理画面「スキルシェア → 予約一覧」の下部に、SPEC 10 が求める CSV を揃えてある。

- 講師の CSV（Googleカレンダーの URL は出力しない。連携の有無のみ）
- 講座の CSV（イメージ画像は URL で出力）
- 予約・決済履歴の CSV（絞り込み条件を引き継ぐ）

いずれも UTF-8 BOM 付きで、Excel でそのまま開ける。
**予約枠（slots）の CSV は用意していない。** SPEC 10 の対象外だが、移行時に過去の枠まで
引き継ぎたい場合は追加が必要。

---

## 3. SPEC 10 の各項目への道筋

### 月額プラン（サブスクリプション）

現在は `mode=payment` の都度決済のみ。サブスクへ進む場合の影響範囲。

- **Stripe 側**：`mode=subscription` と Price オブジェクトが必要。商品を都度作る今のやり方
  （`price_data` をその場で組み立てる）から、事前に登録した Price を参照する形へ変える
- **データ**：`bookings` は1予約1決済の前提。サブスクだと「契約」と「消化した枠」が別概念になるため、
  契約テーブルの追加が要る
- **Webhook**：`invoice.paid` や `customer.subscription.deleted` の処理が増える
- 現在の Webhook は `checkout.session.completed` だけを見て、他は 200 で受け流している。
  イベントの追加は `ssb_rest_stripe_webhook()` の分岐を増やすだけで済む

### Stripe Connect の導入

**このPoCで最も影響が大きい変更。** 現在は「運営が販売主体、受講料は運営が受領」という前提で
作ってある（SPEC 1）。Connect に切り替えると資金の流れが変わる。

- 講師ごとに Connect アカウントの作成とオンボーディングが要る。`instructors` に
  `stripe_account_id` 列を足す
- Checkout セッションに `payment_intent_data[transfer_data][destination]` または
  `on_behalf_of` を付ける。手数料は `application_fee_amount`
- **管理画面の月次売上集計は役目を終える**。振込は Stripe が行うため、集計は参考値になる
- 返金・チャージバックの負担者が変わるため、規約とキャンセルポリシーの見直しが必須
- 現在の実装で `ssb_create_checkout_session()` に手を入れるだけで済むよう、
  セッション作成は1箇所にまとめてある

### インボイス制度対応

- `instructors` に登録番号の列を追加
- 領収書の出し分けが要る。現在は領収書を発行していない（Stripe のレシートのみ）
- Connect を入れるかどうかで「誰が誰に発行するか」が変わるため、Connect と合わせて設計すること

### Google OAuth 連携によるリアルタイム同期

現在は ICS の限定公開URLを読むだけの一方向・1時間キャッシュ。

- 取得部分（`ssb_gcal_fetch()`）を API 呼び出しに差し替えれば、解析以降はそのまま使える
- 双方向にするなら、予約確定時のカレンダー登録を「`.ics` の招待メール」から
  「Events API での作成」へ変更する。`ssb_build_invite_ics()` の呼び出し箇所を置き換える
- Google の審査（機微スコープ）が必要。審査期間を見込むこと
- `instructors.gcal_ics_url` は使わなくなるが、トークン保管用の列が別途要る

### メッセージ・レビュー機能

- 新規テーブルの追加で済み、既存のテーブルには影響しない
- レビューは `bookings` に紐づける（受講した人だけが書ける形）のが自然

---

## 4. 本番公開の前に必要なこと

コードは仕様を満たしているが、公開にはこれらが要る。

| 項目 | 状況 | 必要な作業 |
|---|---|---|
| 規約類の記入 | 雛形のみ | `/tokushoho` の事業者情報は**法令上の必須記載事項**。`/cancel-policy` は段階的返金の雛形のままなので、実装（常に全額返金）に合わせて書き換える |
| Stripe 本番キー | テストキー | 本番アカウントの審査（事業者情報・銀行口座）を通してから差し替え |
| Webhook の本番登録 | `stripe listen` で中継 | ダッシュボードで実ドメインのエンドポイントを登録し、`whsec_` を差し替え |
| メール送信 | `wp_mail()` のまま | 本番のレンタルサーバーからの送信は届かないことが多い。SMTP か SES 等の利用を推奨 |
| エラーログ | PHP のエラーログ | 保存先と保持期間を決めること。決済まわりの失敗はここにしか残らない |
| HTTPS | ローカルは http | 本番は必須（Stripe の要件でもある） |

---

## 5. 意図的に見送った割り切り

PoC として妥当と判断して見送った点。将来つまずきやすい箇所なので明記しておく。

### 仕様どおりスコープ外にしたもの（SPEC 9）

月額プラン、Stripe Connect、メッセージ、レビュー、出金申請、スマホアプリ、多言語、Google OAuth。

### 実装上の割り切り

- **Stripe の公式 SDK を使っていない**。開発機に Composer が無いため、SPEC 4.6 が代替として
  認めている `wp_remote_post()` 方式で実装し、署名検証も自前で書いた。
  検証は11ケースで網羅しているが、SDK へ移行するなら `ssb_stripe_post()` と
  `ssb_verify_stripe_signature()` の2箇所を差し替えれば済む
- **繰り返し予定は `FREQ=DAILY` と `FREQ=WEEKLY` のみ**（SPEC 4.4 の指示どおり簡易対応）。
  月次・年次の繰り返しは1件として扱う
- **キャンセルと自動返金は実装済み**（SPEC 4.7）。ただし返金額は常に全額で、
  キャンセルポリシーに沿った段階的な返金には対応していない
- **ログインの試行回数制限が無い**。総当たり対策が要るなら、プラグインか実装の追加を検討
- **枠の `closed`（停止中）ステータスに UI が無い**。スキーマは SPEC どおり持っているが、
  設定する画面は作っていない。枠を止めたい場合は削除で代用している
- **決済が成立したのに枠を確保できなかった場合**（仮押さえが切れて他の人に取られた等）は、
  予約は確定させたうえでログに残し、運営の手作業に委ねる。
  仮押さえ（35分）を Checkout セッション（32分）より長く保つことで、まず起きないようにしている

---

## 6. 移行時に持っていくべき考え方

作り直しても失われないよう、設計判断の理由を残す。

- **決済の確定は Webhook でしか行わない**。`success_url` の画面は表示のみ。
  ブラウザが戻ってこなくても予約は確定する
- **仮押さえは `SELECT ... FOR UPDATE` を含むトランザクションで取る**。
  同時クリックで二重に取れないことを実機で確認済み
- **期限切れの解放は cron を使わず、枠を読む直前に必ず実行する**。
  cron が動かない環境でも破綻しない
- **金額はフロントから受け取らない**。必ず `courses.price` を参照する
- **`stripe_session_id` は一意制約を張り、未作成の間は NULL を入れる**。
  空文字だと2件目の未決済予約が作れなくなる
- **Googleカレンダーは除外フィルタとしてのみ働く**。連携の失敗で予約を止めない
- **ICS URL は画面にもログにも出さない**。第三者に渡ると予定が閲覧できてしまう
